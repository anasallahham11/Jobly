import 'package:flutter/material.dart';

class SingupEmployee extends StatelessWidget {
  const SingupEmployee({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}